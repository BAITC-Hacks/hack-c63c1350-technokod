# Technokod — HackAlem AI 2026

> Название проекта, кейс и описание будут заполнены после публикации задания (23.09.2026, 12:30).

## Краткое описание
_Какую проблему решает проект и для кого._

## Что реализовано
_Основные функции._

## Как работает решение
_Основной сценарий от входных данных до результата._

## Технологии
Python 3.11, OpenAI API (основной провайдер), NVIDIA NIM API (резервный, совместим с OpenAI SDK), DuckDB, pandas, FastAPI.

## Архитектура
_Компоненты и связи. Схема будет добавлена._

## Установка
```bash
python -m venv .venv
# Windows: .venv\Scripts\activate   Linux/macOS: source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env   # заполнить ключи или включить DEMO_MODE=true
python scripts/check_env.py
```

## Запуск
_Команда запуска будет добавлена._

Через Docker:
```bash
docker compose up --build
```

## Как проверить решение
_Пошаговый сценарий для жюри с ожидаемым результатом. Будет добавлен._

Проверка без ключей: `DEMO_MODE=true` в `.env`, ответы модели для контрольного сценария берутся из `data/cache/demo_responses.json`.

## Данные и интеграции
_Источники данных, API, внешние сервисы._

## Стороннее и подготовленное до старта
Раскрытие по п. 5.4.4 и 5.4.4.2 Положения. До начала соревновательной части (13:00) в репозиторий добавлены только инфраструктурные заготовки, не содержащие логики кейса:
- структура папок, `.gitignore`, `.env.example`, `requirements.txt`, `Dockerfile`, `docker-compose.yml`;
- `app/config.py` — чтение настроек из окружения;
- `app/llm.py` — обёртка над OpenAI SDK с переключением провайдера (OpenAI / NVIDIA NIM) и режимом проверки без ключей;
- `scripts/check_env.py` — проверка окружения;
- `tests/test_smoke.py`.

Open-source библиотеки: см. `requirements.txt`. Модели и API: OpenAI, NVIDIA NIM. Иные сторонние компоненты будут перечислены здесь по мере добавления.

Инструменты разработки: AI-агенты для генерации и анализа кода (разрешено п. 5.4.12 Положения).

## Ограничения
_Что не реализовано, где упрощено._

## Развёрнутая версия
_Ссылка, если появится._
