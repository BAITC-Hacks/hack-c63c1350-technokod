# Архитектура решения

Документ описывает фактический код репозитория. Контракт для пользователя — `README.md`, разбор данных —
`docs/analysis.md`, измеренное качество — `docs/metrics.json` и `docs/metrics.md`, устройство агентного
контура — `docs/agent-design.md`, обоснование решений — `docs/adr/`.

## Назначение и границы

Система выдаёт почасовой прогноз нормализованной выработки (0..1) двух турбин ВЭС в Шелекском коридоре
на 24–48 часов вперёд. Для каждой даты выпуска используется только тот прогноз погоды, который был
доступен на эту дату. Факт погоды и факт выработки прогнозируемых суток не используются нигде — ни в
тесте, ни в обучении.

Что система делает:

- забирает архивный прогноз погоды по координатам ВЭС на заданную дату выпуска;
- строит признаки и считает прогноз выработки по каждой турбине;
- проверяет результат: часы штиля, номинала, шторма, расхождение турбин, изменение относительно
  предыдущего выпуска;
- пересчитывает прогноз, если входные данные обновились или появились штормовые часы;
- воспроизводит всю последовательность выпусков тестового периода и собирает итоговый ряд февраля;
- ведёт журнал действий по каждому выпуску.

Чего система не делает: не планирует ремонты и остановы, не моделирует ограничения сети и команды
диспетчера, не прогнозирует цену и не оптимизирует заявку на рынок, не обслуживает турбины вне двух
заданных координат без переобучения.

## Поток данных

```
ИСТОЧНИКИ                      СЛОИ                                        АРТЕФАКТЫ

data/raw/turbine_1.csv ─┐
data/raw/turbine_2.csv ─┴─► app/data.py
                            load_turbine → to_hourly
                            10 минут → час, час с < 3 точками = пропуск
                                  │  ts, wind_ms, power_norm, temp_c
                                  ▼
previous-runs-api ───┐      app/features.py ◄── app/weather.py ──► data/cache/weather/*.json
historical-forecast  ┴─────►  build_features        forecast_available_at      (кэш, запуск без сети)
        (Open-Meteo)          16 признаков          previous_runs_training
                                  │
                                  ▼
                            app/model.py ──── обучение ──► scripts/train.py ──► models/turbine_*.joblib
                            curve | gbm | two_stage                             docs/metrics.json
                            смесь весов + квантили остатков
                                  │
                                  ▼
                            app/agent.py  ForecastAgent
                            get_weather → check_input_quality → prepare_and_predict
                                       → analyze → [recalculate] → save_forecast  ┌─► outputs/forecasts/<дата>.csv
                            оркестратор: LLM (app/llm.py) либо план ──────────┴─► outputs/agent_logs/<дата>.json
                                  │
                                  ▼
                            app/backtest.py  30 выпусков подряд ──────────────► outputs/forecast_all_issues.csv
                            backtest → build_submission ─────────────────────► outputs/forecast_feb2026.csv
                                  │                                          └─► outputs/backtest_summary.csv
                                  ▼
                            app/cli.py  check | fetch-weather | train | forecast | backtest | report
```

## Модули

| Файл | Ответственность | Ключевые функции | Зависит от |
|---|---|---|---|
| `app/config.py` | настройки и ключи из `.env`, в коде ключей нет | `Settings`, `settings` | python-dotenv |
| `app/data.py` | чтение CSV организаторов, часовая агрегация, пометка пропусков | `load_turbine`, `to_hourly`, `load_hourly` | pandas |
| `app/weather.py` | архивные прогнозы Open-Meteo, перевод км/ч → м/с, дисковый кэш | `forecast_available_at`, `previous_runs`, `previous_runs_training`, `historical_forecast` | httpx |
| `app/features.py` | 16 признаков из прогноза и календаря, стыковка с фактом | `build_features`, `make_training_set`, `FEATURES` | numpy, pandas |
| `app/model.py` | три компонента прогноза, их смесь, интервал P10–P90, метрики | `GenerationModel`, `PowerCurve`, `evaluate`, `level_bin` | scikit-learn, joblib |
| `app/llm.py` | единый клиент LLM, function calling, кэш ответов для режима без ключей | `chat`, `chat_tools`, `available` | openai SDK |
| `app/agent.py` | агентный цикл, шесть инструментов, контроль качества входа и отказ от выпуска, журнал действий | `ForecastAgent`, `AgentState`, `FORECAST_COLUMNS` | model, weather, llm |
| `app/backtest.py` | последовательные выпуски, сборка итогового ряда февраля | `backtest`, `build_submission`, `run` | agent |
| `app/cli.py` | командная строка | `main`, `cmd_*` | agent, backtest, scripts.train |
| `scripts/train.py` | обучение, подбор весов смеси, квантили остатков, запись метрик | `main`, `honest_predictions` | model, weather, data |
| `scripts/check_env.py` | проверка окружения перед запуском | — | config |
| `scripts/demo.sh`, `scripts/demo.ps1` | контрольный сценарий для жюри | — | cli |
| `tests/` | тесты пайплайна и одного выпуска агента | — | pytest |

## Контракты данных

Часовой ряд ВЭС (`app/data.py`):

| Колонка | Смысл |
|---|---|
| `ts` | начало часа, местное время |
| `wind_ms` | средняя скорость ветра по датчику гондолы |
| `power_norm` | нормализованная активная мощность 0..1, целевая переменная |
| `temp_c` | температура воздуха |
| `n_points` | сколько 10-минуток попало в час; при `n_points < 3` значения = NaN |

Погодный кадр (`app/weather.py`, `forecast_available_at`):

| Колонка | Смысл |
|---|---|
| `ts` | час прогноза, местное время `Asia/Almaty` |
| `issue_date` | дата выпуска, на которую этот прогноз был доступен |
| `lead_day` | 1 для часов 1–24 горизонта, 2 для часов 25–48 |
| `wind_speed_100m`, `wind_speed_10m`, `wind_gusts_10m` | скорости в м/с (источник отдаёт км/ч, перевод в `_to_frame`) |
| `wind_direction_100m` | направление, градусы |
| `temperature_2m`, `surface_pressure` | температура °C, приземное давление гПа |

Выпуск прогноза `outputs/forecasts/<issue_date>.csv` — 96 строк (48 часов × 2 турбины), константа
`FORECAST_COLUMNS` в `app/agent.py`:

| Колонка | Смысл |
|---|---|
| `issue_date` | дата выпуска |
| `ts` | час прогноза |
| `horizon_hour` | 1..48, номер часа горизонта |
| `lead_day` | 1 или 2 — за сколько суток выпущен использованный прогноз погоды |
| `turbine` | 1 или 2 |
| `wind_speed_100m` | прогнозная скорость ветра на высоте 100 м, м/с |
| `power_norm_pred` | итоговый прогноз смеси, 0..1 |
| `power_p10`, `power_p90` | границы интервала неопределённости по квантилям остатков валидации |
| `power_gbm_pred`, `power_two_stage_pred`, `power_curve_pred` | три компонента смеси по отдельности |
| `wind_nacelle_pred` | промежуточный выход двухэтапной схемы: прогнозная скорость ветра на гондоле, м/с |

Всего 13 колонок, значения округлены до четырёх знаков. Порядок задаётся `FORECAST_COLUMNS`;
`app/backtest.py` переносит в сводный файл те из них, что присутствуют в выпуске.

Сводные файлы:

| Файл | Строк | Колонки |
|---|---|---|
| `outputs/forecast_all_issues.csv` | 30 × 96 = 2880 | `issue_date, ts, horizon_hour, lead_day, turbine, power_norm_pred, …компоненты` |
| `outputs/forecast_feb2026.csv` | 672 (01.02 00:00 – 28.02 23:00, без пропусков) | `ts, turbine_1_lead1, turbine_2_lead1, turbine_1_lead2, turbine_2_lead2, turbine_1_p10, turbine_2_p10, turbine_1_p90, turbine_2_p90, farm_mean_lead1, farm_mean_lead2, farm_p10_lead1, farm_p90_lead1` |
| `outputs/backtest_summary.csv` | 30 | `issue_date, hours, recomputed, llm_used, storm_hours, calm_hours, rated_hours, mean_cf_t1, mean_cf_t2, mean_abs_change_vs_prev` |
| `outputs/agent_logs/<дата>.json` | 30 файлов | `issue_date, llm_used, published, confidence, quality, recalculated, analysis, conclusion, trace` |
| `outputs/agent_logs_llm/<дата>.json` | 3 файла | те же поля; выпуски 05.02, 15.02 и 21.02, где порядок инструментов выбирала LLM |

Пропусков в итоговом файле нет ни в одной колонке: выпуски начинаются с 29.01, поэтому 01.02 получает
прогноз и на сутки (выпуск от 31.01), и на двое суток (выпуск от 30.01). Два выпуска, 29.01 и 30.01,
лежат вне февраля и нужны именно для этого, а также закрывают часы 31 января — на случай трактовки
«следующих 24–48 часов» с отсчётом внутри 31 января.

## Сквозные решения

**Только прогноз, доступный на момент выпуска.** Единственная точка входа погоды —
`forecast_available_at(site, issue_date, 48)`: часы 1–24 берутся из слоя `*_previous_day1`, часы 25–48 —
из `*_previous_day2`, оба слоя содержат значения прогонов, выпущенных не позже `issue_date`. Обучение
идёт на том же источнике (`previous_runs_training`), поэтому распределения признаков в обучении и в
тесте совпадают. Обоснование — `docs/adr/0001-weather-source.md`.

**Кэш и работа без сети.** Каждый HTTP-запрос кэшируется на диск по хешу «url + параметры» в
`data/cache/weather/`; кэш лежит в репозитории, поэтому обучение, прогноз и бэктест повторяются без
интернета. Шаги оркестрации не кэшируются: в `DEMO_MODE` и без ключа выпуск целиком выполняет
детерминированный планировщик, поэтому числовой результат от наличия ключа не зависит.

**Откат LLM и устойчивость.** Ошибка провайдера — нет ключа, сеть, лимит, некорректный ответ —
переводит выпуск на детерминированный план целиком: в трассе появляется запись `llm_fallback` с
причиной, план заново проходит весь цикл, поэтому частичный результат модели не смешивается с планом.
Ошибка отдельного инструмента провайдером не считается: она возвращается модели как результат вызова
(`{"error": …}`), и модель исправляет порядок сама. Если модель не вызвала `analyze` или
`save_forecast`, это делает страховка в `run_day`, так что выпуск всегда полон. Набор инструментов и
формат результата в обоих режимах одинаковы.

**Часовые пояса.** Данные ВЭС в местном времени, погода запрашивается с `timezone=Asia/Almaty`, стыковка
по локальному часу. Переход Казахстана на UTC+5 в марте 2024 обрабатывается на стороне источника, сдвигов
в коде не требуется.

**Пропуски.** Часы с менее чем тремя 10-минутными записями помечаются пропуском и не интерполируются:
`make_training_set` делает inner join и `dropna`, поэтому в обучение попадают только часы с фактом
(T1 — 1 649 часов без факта, T2 — 449, разбор в `docs/data-quality.md`). Прогноз при этом считается на все 48 часов горизонта.

**Воспроизводимость.** Случайность зафиксирована (`random_state=42`), веса смеси и квантили остатков
хранятся внутри `models/turbine_*.joblib`, метрики пишет тот же скрипт, который обучает модель
(`scripts/train.py` → `docs/metrics.json`).

## Масштабирование на реальную ВЭС

| Направление | Что меняется |
|---|---|
| Больше турбин | модель на турбину обучается независимо (`GenerationModel(turbine)`), агент принимает список турбин параметром; при разносе больше ~1 км одной точки погоды мало — нужен запрос по сетке и привязка турбины к ближайшему узлу |
| Четыре выпуска в сутки | ключом выпуска становится `issue_time`, а не `issue_date`; архивный слой `previous_dayN` заменяется оперативным прогнозом того же провайдера, остальной контур не меняется |
| Хранение | CSV в `outputs/` заменяется таблицей прогнозов (issue_time, ts, turbine, значение, версия модели); выпуски версионируются, старые не перезаписываются |
| Мониторинг качества | ежедневная сверка выпуска с фактом SCADA, скользящий nMAE по горизонтам, алерт при дрейфе выше порога, переобучение по расписанию |
| Отказоустойчивость | резервный провайдер погоды, резервный провайдер LLM (переключение `openai` / `nvidia` уже в коде), кэш как последняя линия обороны |
