# Данные

- `samples/` — тестовые данные, которые лежат в репозитории и нужны для контрольного сценария.
- `raw/` — исходные данные кейса (не коммитятся, если организаторы не разрешили).
- `cache/demo_responses.json` — сохранённые ответы модели для проверки без ключей (DEMO_MODE=true).
