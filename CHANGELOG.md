# История изменений

Формат по [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/), нумерация по [SemVer](https://semver.org/lang/ru/).
Время местное (Астана, UTC+5), сверено с `git log`.

## [0.1.0] — 23.09.2026

Первая версия решения WindAgent, подготовленная в рамках HackAlem AI 2026 (кейс Самрук-Казына «Agentic AI для прогнозирования выработки ВЭС»).

### Добавлено

- **13:32** `b2b3b5b` — разбор ТЗ и данных (`docs/analysis.md`), дорожная карта (`docs/roadmap.md`), загрузчик данных ВЭС с часовой агрегацией и пометкой пропусков (`app/data.py`), инструмент архивных прогнозов Open-Meteo с кэшем на диск (`app/weather.py`), датасеты организаторов в репозитории.
- **13:37** `532a10d` — признаки из прогноза погоды и календаря (`app/features.py`), модель выработки: эмпирическая кривая мощности плюс градиентный бустинг (`app/model.py`), обучение с честной валидацией января 2026 (`scripts/train.py`), метрики в `docs/metrics.json`.
- **13:39** `f385b17` — агент прогнозирования с пятью инструментами, анализом и пересчётом (`app/agent.py`), бэктест по дням февраля 2026 (`app/backtest.py`), командный интерфейс (`app/cli.py`).
- **13:40** `097362a` — тесты основного сценария (`tests/test_pipeline.py`), контрольные скрипты для жюри `scripts/demo.sh` и `scripts/demo.ps1`.
- **13:41** `872fd9e` — README: описание решения, установка, порядок проверки, данные и интеграции, раскрытие подготовленного до старта, ограничения.
- **13:45** `9c7043b` — двухэтапная модель (прогноз погоды → скорость ветра на гондоле → измеренная кривая мощности турбины), смесь трёх компонентов с подбором весов на валидации, сводка метрик в `docs/metrics.md`.
- **13:45** `3bad27d` — оркестрация агента через function calling (`app.llm.chat_tools`), проверка доступности провайдера (`available()`), `.gitattributes` для единых окончаний строк.
- **13:48** `6bc3e12` — кэш архивных прогнозов включён в репозиторий (контрольный сценарий работает без сети), метрики пересчитаны на чистой установке.
- Проектная документация: архитектура и журнал архитектурных решений, устройство агентного контура, карточка модели, протокол валидации, отчёт о качестве данных, ценность для бизнеса, руководство пользователя, эксплуатационный регламент, программа и методика испытаний, карта документации, раскрытие по правилам хакатона — см. `docs/index.md`.

### Изменено

- **13:42** `9fd98b0` — `scripts/check_env.py`: отсутствие ключа LLM больше не считается ошибкой, контрольный сценарий проходит без ключей.
- **13:45** `9c7043b` — состав смеси модели: вес GBM «признаки → мощность» снижен до нуля по результатам валидации, основу дают двухэтапная модель и кривая мощности.
- **13:48** `6bc3e12` — `.gitignore`: `data/cache/weather/` и итоговые артефакты `outputs/` теперь версионируются.

### Подготовлено до старта хакатона

Раскрытие по п. 5.4.4 Положения, подробности в `docs/compliance.md`.

- **13:24** `f992796` — инфраструктурный каркас без логики кейса: структура пакета, чтение конфигурации (`app/config.py`), обёртка над OpenAI SDK с переключением провайдера и кэшем ответов (`app/llm.py`), проверка окружения (`scripts/check_env.py`), smoke-тест импортов, `requirements.txt`, `Dockerfile`, `docker-compose.yml`, `.env.example`, `.gitignore`.
- **13:24** `573e6b8` — `scripts/check_env.py`: запуск из любой рабочей директории.
- **06.09.2026 13:51** `7f20735` — начальный коммит репозитория, создан организаторами.

### Известные ограничения версии

- Архив Open-Meteo Previous Runs начинается с 16.02.2024, поэтому обучение идёт на 23 месяцах, а не на всей истории с марта 2023.
- Факт выработки за февраль 2026 участникам не выдан: качество на тестовом периоде не измерено, метрики приведены по январю 2026 в том же протоколе.
- Плановые остановы и ограничения по команде диспетчера не моделируются: в исходных данных нет журнала событий ВЭС.
