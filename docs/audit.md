# Аудит соответствия: ТЗ кейса и положение хакатона

**Дата:** 23.09.2026, 14:06–14:20 (Астана) · **Коммит:** `50af740` (+ незакоммиченные правки README.md и docs/architecture.md)
**Проверял:** независимый надзор, отдельно от исполнителей. Каждая строка подтверждена командой или чтением файла, а не доверием к README.

Проверка сквозная: сверяется не «что написано», а «что делает код и что лежит в артефактах».

## 1. Соответствие задаче (вес 25)

| Требование | Источник | Как проверено | Факт | Статус |
|---|---|---|---|---|
| Горизонт 24–48 часов, шаг час | ТЗ | `forecast_available_at(FARM, "2026-02-05", 48)` | 48 часов, шаг ровно 01:00:00, лиды [1, 2] | выполнено |
| Две турбины | ТЗ | колонка `turbine` в артефактах | значения 1 и 2, по 48 часов на каждую | выполнено |
| Тестовый период 01.02–28.02.2026 покрыт | ТЗ | `forecast_feb2026.csv` против явной часовой сетки | 672 строки, сетка совпадает полностью, дублей 0 | выполнено |
| Используется прогноз, доступный на дату выпуска, а не факт | ТЗ, ключевое | разбор `FEATURES`, `app/model.py`, `app/weather.py` | В признаках только прогнозные поля (ветер 100 и 10 м, порывы, направление, температура, давление), производные от них, календарь и лид. Факт (`wind_ms`, `power_norm`) встречается исключительно в `GenerationModel.fit` как цель обучения на истории — на предсказании недоступен | выполнено |
| Погодный кадр на предсказании не содержит факта | ТЗ | колонки кадра `forecast_available_at` | `ts, issue_date, lead_day` + шесть прогнозных полей; фактических колонок нет | выполнено |
| Полный агентный цикл с пересчётом | ТЗ | трассы 31 журнала | в трассах встречаются все пять инструментов: `get_weather`, `prepare_and_predict`, `analyze`, `recalculate`, `save_forecast` | выполнено |
| Пересчёт срабатывает реально, а не декларативно | ТЗ | `backtest_summary.csv`, журналы | пересчёт в 4 выпусках из 28: 06.02, 15.02, 21.02, 24.02 | выполнено |
| Порядок инструментов выбирает модель | ТЗ (Agentic AI) | `outputs/agent_logs_llm/*.json` | 3 выпуска с `llm_used: true`; на 15.02 и 21.02 модель сама вызвала пересчёт: `get_weather → prepare_and_predict → analyze → prepare_and_predict → recalculate → save_forecast` | выполнено |

## 2. Техническое исполнение (вес 25)

| Требование | Как проверено | Факт | Статус |
|---|---|---|---|
| Целостность сводной таблицы выпусков | чтение `forecast_all_issues.csv` | 2 688 строк, 28 выпусков, `horizon_hour` от 1 до 48, даты выпусков 31.01–27.02 | выполнено |
| Файлы отдельных выпусков | 28 файлов `outputs/forecasts/*.csv` | у всех ровно 96 строк | выполнено |
| Значения прогноза физичны | проверка диапазона | значений вне [0, 1] нет | выполнено |
| Пропуски только обоснованные | подсчёт пропусков | lead1 без пропусков; lead2 пуст ровно за 1 февраля (24 часа × 2 турбины = 48 ячеек) — выпуск от 30.01 не существует | выполнено |
| Сводка по выпускам | `backtest_summary.csv` | 28 строк, заполнены отметки пересчёта и средние КИУМ | выполнено |
| Откат при отказе провайдера LLM | запуск с заведомо неверным ключом | `llm_used: False`, в трассе первым `llm_fallback`, затем полный цикл, выпуск на 96 строк состоялся | выполнено |
| Автотесты | `pytest -q tests` | 6 passed | выполнено |

## 3. README и воспроизводимость (вес 25)

| Утверждение README | Как проверено | Факт | Статус |
|---|---|---|---|
| Кэш прогнозов 69 файлов, 4.9 МБ | `ls`, `du` | 69 файлов, 4.9 МБ | выполнено |
| Контрольный сценарий из семи шагов | `scripts/demo.sh` | шаги `[1/7]`…`[7/7]`, седьмой пропускается без ключа | выполнено |
| Команды CLI | `app.cli check`, `app.cli report` | обе завершаются с кодом 0 | выполнено |
| Все ссылки на документы рабочие | проверка 17 ссылок README | все файлы существуют | выполнено |
| Секретов в репозитории нет | `git grep` по шаблонам ключей среди отслеживаемых файлов | совпадения только на плейсхолдере `sk-...` в руководстве и на коде, читающем переменную; настоящих ключей нет | выполнено |
| `.env` не попадает в репозиторий | `git ls-files`, `git check-ignore` | в индексе отсутствует, игнорируется правилом `.gitignore:2` | выполнено |
| Посторонних и мусорных файлов нет | `git status --ignored` | вне `.venv`, `__pycache__`, `models/`, `.env` игнорируемого мусора нет | выполнено |
| Работа в Docker | — | Docker на машине сборки не установлен, путь через контейнер проверить нельзя. В README и здесь помечено честно | не проверяемо |
| Резервный провайдер NVIDIA NIM | — | ключа нет, путь не проверялся; помечено в README | не проверяемо |

## 4. Положение хакатона

**Блок ожидает данных.** На момент проверки `docs/submission-checklist.md` отсутствует — исполнитель, снимающий положение через браузер, ещё не завершил работу. Требования к составу сдачи (презентация, видео, форма на платформе, состав команды, дедлайн подачи) не подтверждены первоисточником и здесь сознательно не домысливаются. После появления файла раздел заполняется и статусы проставляются.

## 5. Риски потери баллов

По убыванию значимости.

| Риск | Под угрозой | Суть | Что сделать |
|---|---|---|---|
| Требования положения не сверены | вся сдача | Неизвестно, требуются ли презентация, видео, публичность репозитория, форма с составом команды и до какого часа | Дождаться `docs/submission-checklist.md`, затем закрыть недостающее |
| Фактическая ошибка в `docs/adr/0003-nacelle-wind-two-stage.md` | техническое исполнение | Утверждается, что двухстадийная схема — лучший одиночный компонент на T1 (14.46 % против 14.71 % у кривой). По текущему `metrics.json` она хуже кривой: 14.93 % против 14.71 %. Вывод ADR перевёрнут относительно данных | Пересчитать вывод ADR по текущим числам |
| Устаревшая таблица в `docs/adr/0002-model-blend.md` | техническое исполнение | Приведены веса T1 `0.0 / 0.6 / 0.4` и метрики 14.10 / 16.18 — числа промежуточного прогона. Актуально: веса `0.0 / 0.5 / 0.5`, метрики 14.31 / 16.33 | Обновить таблицу |
| Устаревший ориентир в `docs/runbook.md` (строка 94) | воспроизводимость | «nMAE ≈ 14.6–14.7 % на 24 ч, ≈ 16.8–16.9 % на 48 ч» против фактических 14.31–14.55 % и 16.33–16.66 % | Привести к `metrics.json` |
| Публичность репозитория не подтверждена | вся сдача | Проверить из этой среды нечем: `gh` не установлен | Убедиться, что жюри имеет доступ к репозиторию |

Три документных расхождения из пяти относятся к файлам, которые в момент проверки правил исполнитель синхронизации документации, — возможно, часть уже устранена; при повторном прогоне статус уточняется.

## 6. Что может сделать только человек

| Действие | Почему нельзя автоматизировать | Срок |
|---|---|---|
| Подать решение на платформе хакатона | Требует авторизации участника | до дедлайна сдачи |
| Подготовить и загрузить презентацию | Требование положения не подтверждено; материал для неё готов в `docs/pitch.md` | до дедлайна |
| Записать демонстрационное видео, если оно требуется | Требуется участник в кадре и голос | до дедлайна |
| Указать состав команды | Персональные данные участников | до дедлайна |
| Подтвердить доступ жюри к репозиторию | Управление правами в организации GitHub | до дедлайна |
| Отозвать ключ OpenAI после хакатона | Владелец аккаунта | после сдачи |

## 7. История проверок

| Время | Коммит | Проверено | Итог |
|---|---|---|---|
| 23.09.2026 14:06–14:20 | `50af740` | 26 требований | 24 выполнено, 2 не проверяемо (Docker, NVIDIA), блок положения ожидает данных; 4 документных расхождения, критичных дефектов кода не найдено |
